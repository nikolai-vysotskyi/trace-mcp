# Supply-chain quarantine, same as the root .npmrc. npm/pnpm read .npmrc from
# the install directory only — they do NOT walk up to the repo root — so a
# package with its own lockfile needs its own copy or it resolves fresh
# publishes with no waiting period at all (TRA-519).
min-release-age=7
minimum-release-age=10080
