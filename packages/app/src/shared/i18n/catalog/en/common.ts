/* Strings with no single home: words the whole app reuses. Anything that
   belongs to one surface goes in that surface's namespace instead — a common
   namespace that collects everything is how catalogues become unreviewable. */

export const common = {
  never: 'never',
} as const;
