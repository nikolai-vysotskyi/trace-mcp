Review screenshots for TRA-429 (PR: the sidebar update card on the Lattice layer).
Not merged into master; this branch exists only so the PR body can show images.
Shot from the real Electron window per DESIGN.md section 9, hidden, at 2x.
